
module: huggingface.dataset.DatasetModule


dataset: 
  path: "wikitext"
  # name: "cola"
  split: [validation]
  

# submodules: 
#   algocean: get_cfg(ocean.module)
pipeline: 
  - 
    tokenizer: 'bert-base-cased'
    params:
      padding: max_length
      truncation: True 

metric: ['squad']

client: {}